# Stripe History icon

The owner selected 🦓 for hraness.com/stripe. The favicons are rendered from the canonical mark in `public/marks/stripe-history.svg`: `app/icon.png` is 32×32 and `app/apple-icon.png` is 180×180. Hraness’s parent brand is unchanged.
